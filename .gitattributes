# One repo, two machines (D22). Normalise line endings so a Windows checkout
# does not turn every file into a diff.
* text=auto eol=lf

# Vendored binary asset — never touched by EOL normalisation (D35).
*.onnx binary
*.wav binary
